package de.servicehealth.poppmodule.demo.model

/** How the PoPP-Module is integrated into the host app. Each mode owns a route + destination. */
enum class IntegrationMode { INTEGRATED, APP_TO_APP }
